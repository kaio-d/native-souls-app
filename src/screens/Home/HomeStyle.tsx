import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center'
  },
  h1: {
    color: '#fff',
    fontSize: 36,
    fontWeight: 'bold'
  },

  imageConfig: {
    marginTop: 60,
    marginBottom: 120,
  }
});